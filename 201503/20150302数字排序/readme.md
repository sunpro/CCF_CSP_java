## 定义字段
int length //接收数字的个数
int[length] num;//接收的数字
int max;//输入数字的最大值
int[max] count;//次数计数
int[max] index;//用于记录数值；
//接收输入同时确定最大值max

//初始化count index;

//遍历num,将数值加到对应的count中
//
//给count排序，同时将index做同样的变化;
//输出index和count
12
5 2 3 3 1 3 4 2 5 2 3 6